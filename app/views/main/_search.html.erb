<h3>Search for users</h3>
<%= form_tag(search_path, :method => 'get') do %>
  <%= label_tag(:q, 'Search for a user by their name or email address below.') %><br/>
  <%= text_field_tag(:q) %>
  <%= submit_tag("Search") %>
<% end %>
