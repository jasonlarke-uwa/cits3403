<p>Welcome <%= @first_name %> <%= @last_name %>!</p>

<p>You can confirm your account email through the link below:</p>
<p><%= link_to 'Confirm my account', { :only_path => false, :controller => "devise/confirmations", :action => :create, :confirmation_token => @resource.confirmation_token } %>
